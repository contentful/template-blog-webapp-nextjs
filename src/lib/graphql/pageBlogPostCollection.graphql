query pageBlogPostCollection(
  $locale: String
  $preview: Boolean
  $limit: Int
  $order: [PageBlogPostOrder]
) {
  pageBlogPostCollection(limit: $limit, locale: $locale, preview: $preview, order: $order) {
    items {
      ...PageBlogPostFields
    }
  }
}
